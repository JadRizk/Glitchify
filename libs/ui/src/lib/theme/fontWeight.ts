export type FontWeight = {
  extraLight: number;
  light: number;
  regular: number;
  medium: number;
  bold: number;
};

export const fontWeights: FontWeight = {
  extraLight: 200,
  light: 300,
  regular: 400,
  medium: 500,
  bold: 700,
};
