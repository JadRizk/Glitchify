import { RemNumber } from '../utils/RemValue';

export type LineHeights = {
  scale01: RemNumber;
  scale02: RemNumber;
  scale03: RemNumber;
  scale04: RemNumber;
  scale05: RemNumber;
  scale06: RemNumber;
  scale07: RemNumber;
  scale08: RemNumber;
  scale09: RemNumber;
};

export const lineHeights: LineHeights = {
  scale01: '2rem',
  scale02: '2.2rem',
  scale03: '2.4rem',
  scale04: '2.8rem',
  scale05: '3.2rem',
  scale06: '3.8rem',
  scale07: '4.6rem',
  scale08: '5.4rem',
  scale09: '6.4rem',
};
